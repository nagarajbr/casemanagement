<fieldset>
<section class="solidbody">
	<legend><%= set_subheader("Enter address information for school: #{@school.school_name}") %></legend>

	    <% if session[:SCHOOLS_ID].present? %>
	  		<%= form_for @address, url: create_schools_address_path, method: :post do |f| %>
		     	<%= render "shared/common_error", object_argument: @addresses_error_messages %>
		     	<%= render './addresses/form',arg_object: f,arg_address:@address,arg_entity_type: @entity_type%>
		     	 <p>
			        <%= link_to 'Cancel', create_schools_address_path, class: "secondary button radius" %>
			        <%= f.submit "Save", class: 'button radius' %>
			    </p>
	     	<% end %>
	    <% else %>
	    <h5 class="subheader">School is not selected, Select a school by clicking Search button.</h5><br>
	    	<p></p>
	    	<%= link_to 'Search', schools_search_path,class: "button radius" %>
		<% end %>
</section>
</fieldset>