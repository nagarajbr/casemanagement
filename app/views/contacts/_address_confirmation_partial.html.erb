<div class="row">
	<div class="large-2 columns">
	</div>
	<div class="large-4 columns">
		<%= render arg_partial,arg_addr: arg_address %>
	</div>

	<div class="large-4 columns">
		<%= render arg_partial,arg_addr: arg_ups_address %>
	</div>
	<div class="large-2 columns">
	</div>
</div>

<ul>
	<fieldset class="radio">

	  	<li class="required">
	  		<%= arg_object.label arg_flag,"Do you want to overwrite #{addr_type} address with verified address?" %>
		</li>

		<div class="button-group">
	    	<label class="radio-button"><%= arg_object.radio_button arg_flag, "Y" %>Yes</label>
	    	<label class="radio-button"><%= arg_object.radio_button arg_flag, "N"%>No</label>
		</div>
	</fieldset>
</ul>