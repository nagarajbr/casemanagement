<fieldset>
<section class="solidbody">
	<legend><%= set_subheader("Edit representative information of program unit of client: #{client_full_name(@client)} ") %></legend>
	<p>
	<%= render 'program_wizards/selected_completed_program_unit',arg_selected_program_unit: @selected_program_unit %>
</p>
<%= form_for(@program_unit_representative, :url => program_unit_representatives_update_path, method: :patch) do |f| %>
  <%= render "shared/common_error", object_argument: @program_unit_representative %>
    	<ul>
	     	<div class="row">
	          <div class="large-4 columns">
	          	<br>
	               <%= render 'form',arg_object: f,arg_pgu_member_collection:@program_unit_members_collection_for_representatives %>

	               <!-- <li class="required">
					  <%#= f.label :status %>
					  <%#= f.select :status, CodetableItem.item_list(169,"Status").collect { |p| ["#{p.short_description} ", p.id] },{ :include_blank => 'Select a status' }%>
					 </li> -->
		 	  </div>
		    </div>
		</ul>
	    	<%= link_to 'Back', program_unit_representatives_show_path(@program_unit_representative.id) ,class: "secondary button radius" %>
	       	<%= f.submit "Save",class: 'button radius'%>
<% end %>
</section>
</fieldset>